# 📊 D5：降维与特征选择对比报告

> **项目**: HAR-Project
>
> **阶段**: D5 - 特征选择与降维分析
>
> **数据集**: `feature_matrix.csv` (2430 Samples, 14 Features)
>
> **生成时间**: 2026-06-22

---

## 1. 特征重要性分析 (Feature Importance)

### 📈 可视化结果

![Feature Importance](../reports/figures/feature_importance_d5.png)

### 🔍 深度解读

- **核心特征 (Top 4)**: `sma`, `rms_mag`, `std_mag`, `ptp_mag`。
  - **物理意义**: 这四者均反映了运动的**总能量与波动强度**。在人体活动识别中，静止、走路、跑步的能量差异最显著，因此 `sma` 以绝对优势占据首位（权重 > 0.25）。前四个特征累计贡献了超过 80% 的重要性。
- **次要特征**: `skewness_mag`, `mean_z/y/x`。
  - **物理意义**: 反映了动作的分布形态和三轴重力分量。虽然重要性不如能量特征，但在区分细微动作模式时仍起辅助作用。
- **低贡献特征**: `kurtosis_mag`, `dom_freq`, `spectral_centroid` 等频域特征。
  - **原因**: 当前提取的频域特征区分度较低，可能是因为不同动作的主频重叠度高，或者现有的频率特征提取不够精细。
- **⚠️ 数据清洗确认**: 经检查，已排除 `subject_id` 列作为特征输入，避免了模型利用样本 ID 进行“作弊”导致的数据泄露风险。

---

## 2. PCA 降维分析 (Principal Component Analysis)

### 📉 累计方差贡献率

![PCA Cumulative Variance](../reports/figures/pca_variance_d5.png)

### 🔍 深度解读

- **关键阈值**: 曲线在 **X=9** 处突破 **95%** 的累计方差解释率。
- **冗余性分析**: 原始 14 维特征中存在一定的线性相关性。相比之前的预估，实际数据的信息密度略低，需要更多主成分来保留完整信息。
- **降维潜力**:
  - 理论上仅需 **9 个主成分** 即可替代原始 14 维特征，且保留绝大部分判别信息。
  - 虽然压缩比不如预期极端，但去除 5 个冗余维度仍能有效降低计算量，为嵌入式部署提供支撑。

---

## 3. t-SNE 流形可视化 (t-SNE Visualization)

### 🎨 二维空间聚类

![t-SNE Visualization](../reports/figures/tsne_d5.png)

### 🔍 深度解读

- **类间分离度 (Inter-class Separation)**:
  - 6 个类别在二维平面上形成了 **明显的簇结构**。
  - 左侧簇（Class 1, 2, 3）与右侧簇（Class 0, 4, 5）之间存在巨大的“真空地带”，说明这两大类活动在能量级上有本质区别（如静止 vs 运动）。
- **类内紧凑度 (Intra-class Compactness)**:
  - 同类样本点紧密抱团，说明特征提取的一致性极高，噪声干扰小。
- **局部混杂分析**:
  - 在左侧簇内部，Class 1（绿色）、Class 2（紫色）、Class 3（粉色）存在一定程度的交织。这解释了为何模型在区分这些相似动作（如上楼、下楼、站立）时可能出现少量误判。
- **模型上限预判**:
  - t-SNE 证实数据在特征空间中具有极佳的可分性。既然肉眼在 2D 平面都能轻松分开主要类别，说明**当前特征空间质量很高**，模型性能瓶颈可能在于对相似动作的细粒度区分。

---

## 4. 总结与后续建议

### ✅ 核心结论

1. **特征工程质量高**: `sma` 等能量特征是核心驱动力，信噪比优秀。
2. **降维空间适中**: PCA 证实数据内在维度约为 9 维，具备约 35% 的压缩潜力。
3. **分类逻辑清晰**: t-SNE 揭示了数据天然的“动静二分”结构，以及相似动作间的局部模糊区。

### 🚀 下一步行动 (D6 规划)

1. **对比实验**: 在 D6 中系统对比 `全量14维` vs `Top-7特征` vs `PCA-9维` 的准确率与训练耗时。
2. **针对性优化**: 针对 t-SNE 中左侧簇的混杂现象，尝试引入更能区分“姿态变化”的特征（如三轴相关性、频域熵），以提升相似动作的识别率。
3. **模型轻量化**: 尝试使用 PCA 降维后的数据训练轻量级模型（如 Logistic Regression 或 SVM），验证是否能在保持 90%+ 准确率的同时，大幅降低推理延迟。

---

*Report generated by `feature_selection.py`*